apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "my-applicationArgoCD.fullname" . }}-test-connection"
  labels:
    {{- include "my-applicationArgoCD.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "my-applicationArgoCD.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
