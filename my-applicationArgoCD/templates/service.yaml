apiVersion: v1
kind: Service
metadata:
  name: {{ include "my-applicationArgoCD.fullname" . }}
  labels:
    {{- include "my-applicationArgoCD.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
      name: http
  selector:
    {{- include "my-applicationArgoCD.selectorLabels" . | nindent 4 }}
